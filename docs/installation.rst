============
Installation
============

At the command line::

    $ easy_install django-rubricate

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-rubricate
    $ pip install django-rubricate
